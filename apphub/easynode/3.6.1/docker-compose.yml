services:
  easynode:
    image: "chaoszhu/easynode:v3.6.1"
    deploy:
      resources:
        limits:
          cpus: ${CPUS}
          memory: ${MEMORY_LIMIT}
    restart: always
    networks:
      - baota_net
    ports:
      - "${HOST_IP}:${WEB_PORT}:8082"
    volumes:
      - ${APP_PATH}/data:/easynode/app/db
    labels:
      createdBy: "bt_apps"

networks:
  baota_net:
    external: true
