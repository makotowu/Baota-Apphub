services:
  frp-panel:
    restart: always
    network_mode: host
    command: ${COMMAND_LINE}
    image: vaalacat/frp-panel:v0.1.36
    deploy:
      resources:
        limits:
          cpus: ${CPUS}
          memory: ${MEMORY_LIMIT}
    labels:
      createdBy: "bt_apps"
